import React from 'react'

/* By default everything in each file is private is by default, Hence we need to export it
We can export w/o using the keyboard default, but doing so helps the process*/
export default function HomePage() {
    return (
        <div className="jumbotron">
            <h1>Administration</h1>
        </div>
    )
}
 